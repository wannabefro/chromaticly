# fyu.13 — the Grade 4 end-to-end journey (DoD): a REAL learner (no dev seed, no
# cleared exam) walks the outer loop and enters the first real Grade 4 unit. This
# proves level4() is real and Grade 4 is reachable without the ?seed= dev seam —
# the invariant the per-strand grade4-*.yaml flows (which DO use the seam) can't
# prove.
#
# Since U11 the learner no longer PICKS Grade 4 at onboarding: there is no grade
# pill, and placement seeds seven independent depths (R1). The grade-4 claim is
# made further down, where it is now real — the lane picker opens grade 4
# explicitly and keys-4 is entered.
#
# The 3-item coached warm-up is deliberately grade-1 (note_value_compare seeds
# 0..2, CoachedWarmUp D11) regardless of the picked grade, so the correct option
# indices stay 0, 1, 1 exactly as in onboarding-first-set.yaml.
#
# Prerequisite: Metro running, Chromaticly active in Expo Go (see README).
#   maestro test -e DEV_URL=exp://127.0.0.1:8090 .maestro/grade4-journey.yaml
appId: host.exp.Exponent
---
- clearState
- openLink: ${DEV_URL}
- runFlow: _dev-menu.yaml
- extendedWaitUntil:
    visible: { id: "welcome-screen" }
    timeout: 60000

# Welcome → placement (no grade pill since U11 — placement seeds seven depths)
- tapOn: { id: "start-learning" }
- assertVisible: { id: "placement-intro" }
- tapOn: { id: "placement-start" }
- assertVisible: { id: "placement-item" }

# Looped on the item being on screen, not on a count. Every tap is optional, so
# one body absorbs all three input shapes the bank serves.
- repeat:
    while:
      visible: { id: "placement-item" }
    commands:
      - tapOn: { id: "option-0", optional: true }
      # The context item's bar strip sits below the passage card, off-screen on a
      # phone. It is a native Pressable, not part of the abcjs WebView, so it is
      # tappable — but only once scrolled to.
      - scrollUntilVisible:
          element: { id: "bar-1" }
          direction: DOWN
          visibilityPercentage: 60
          timeout: 4000
          optional: true
      - tapOn: { id: "bar-1", optional: true }
      - tapOn: { id: "roman-numeral-I", optional: true }
      - tapOn: { id: "check", optional: true }
      - tapOn: { id: "feedback-sheet-continue", optional: true }

- extendedWaitUntil:
    visible: { id: "placement-result" }
    timeout: 15000
- tapOn: { id: "placement-accept" }
- assertVisible: { id: "plan-screen" }

# Your plan → coached warm-up (grade-1 items; indices 0, 1, 1)
- tapOn: { id: "plan-start-warmup" }
- assertVisible: { id: "warm-up-screen" }
# The three coached questions, answered by LABEL rather than by index.
# `warmUpFor` owns the seeds, and a seed change reshuffles the options — the
# old `option-0 / option-1 / option-1` comment was already wrong when the
# warm-up atom moved to `add_time_signature` (chromaticly-atz), and
# retry-until-correct turned that into a silent stall on question 3 of 3.
- tapOn: { text: "3/4" }
- tapOn: { id: "check" }
- tapOn: { id: "feedback-sheet-continue" }
- tapOn: { text: "2/4" }
- tapOn: { id: "check" }
- tapOn: { id: "feedback-sheet-continue" }
- tapOn: { text: "4/4" }
- tapOn: { id: "check" }
- tapOn: { id: "feedback-sheet-continue" }

# Landed → the Grade 4 home
- extendedWaitUntil:
    visible: { id: "landed-screen" }
    timeout: 15000
- tapOn: { id: "landed-continue" }

# Onboarding lands on the seven-lane Learn tab (7a), not the level map.
- extendedWaitUntil:
    visible: { id: "lanes-screen" }
    timeout: 15000

# The Profile working-grade assertion is GONE, not moved. G6 U8 deleted that row
# with the grade pill: a learner has seven lane depths and no single current
# grade, and `Profile.grade` survives only as hidden onboarding state that
# nothing renders. `readiness-drill.yaml` and `ProfileScreen.test.tsx` both
# assert its absence, so this flow was the last place claiming it exists.
#
# Nothing is lost. The grade-4 claim is made below, where it is now real: the
# lane picker opens grade 4 explicitly and `keys-4` is entered.

# keys-4 is a SCALES & KEYS unit at grade 4. Onboarding asserts a grade but
# produces no evidence, so the lane opens at its first content-bearing grade and
# grade 4 is chosen explicitly — which is the honest reading under R1: a selected
# grade is not a held one. Nothing refuses entry (R2).
- extendedWaitUntil:
    visible: { id: "lanes-screen" }
    timeout: 15000
- tapOn: { id: "lane-row-scales_keys" }
- extendedWaitUntil:
    visible: { id: "lane-screen" }
    timeout: 15000
# The picker toggles and may already be open (chromaticly-58e).
- runFlow:
    when:
      notVisible: { id: "lane-grade-picker" }
    commands:
      - tapOn: { id: "lane-other-grades" }
- extendedWaitUntil:
    visible: { id: "lane-grade-picker" }
    timeout: 10000
- tapOn: { id: "lane-grade-option-4" }
- extendedWaitUntil:
    visible: { id: "unit-row-keys-4" }
    timeout: 15000
- tapOn: { id: "unit-row-keys-4" }
- assertVisible: { id: "set-runner" }
- takeScreenshot: g4-journey-set
