Full-width CTA button; use `strand` so the button matches the screen's strand hue, `variant="exam"` inside assessment mode only.

```jsx
<Button label="Check" strand="pitch" />
<Button label="Review 1 mistake" variant="secondary" />
<Button label="Begin exam" variant="exam" />
```

Disabled (`disabled`) is the pre-selection state of "Check".
