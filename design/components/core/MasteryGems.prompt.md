Set-complete summary row; the hint button in the exercise footer "dims the mastery gem for this item" — that's the `hinted` state.

```jsx
<MasteryGems items={['clean','clean','hinted','clean','missed','clean']} hue="#2fbfae" />
```
