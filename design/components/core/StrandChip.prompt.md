Identifies the current strand at the top of exercises, lessons and unit rows. Exports `STRAND_DEFS` (hue/glyph/label per strand) for building custom strand-coloured UI.

```jsx
<StrandChip strand="pitch" />
<StrandChip strand="rhythm" showGlyph />
```
