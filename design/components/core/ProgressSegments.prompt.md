Sits in the exercise header between close (×) and the count. Also used as the exam section navigator with `register="exam"`.

```jsx
<ProgressSegments states={['done','done','incorrect','current','todo','todo','todo','todo']} strand="pitch" />
```
