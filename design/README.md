# Design

Exported from the Claude Design project **Chromaticly Music Theory App**
(core flows prototype).

- Project: https://claude.ai/design/p/23f48db1-2662-478d-961d-dc0659bf7224
- `Chromaticly Core Flows.dc.html` — interactive prototype of the core flows.
- `support.js` — generated runtime bundle the prototype loads (do not hand-edit;
  it is rebuilt from `dc-runtime` upstream).

The prototype's preview screenshots (`dash.png`, `check.png`, `v2.png`) live in
the Design project only — they're binary and can't be exported through the
text-based Design API. Grab them from the project link above if needed.
